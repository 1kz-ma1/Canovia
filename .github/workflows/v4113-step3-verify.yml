name: V41.13 Step3 Verify

on:
  pull_request:
    branches: [main]

jobs:
  focused:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, sqlite, pdo_sqlite
          coverage: none
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist --no-progress
      - name: Build frontend
        run: |
          npm ci
          npm run build
      - name: Syntax and Blade
        run: |
          php -l app/Services/NativeAiGateway.php
          php -l app/Services/InboxIntelligenceService.php
          php -l app/Services/InboxRoutingService.php
          php -l app/Http/Controllers/InboxController.php
          php -l config/canovia_guides.php
          php -l tests/Feature/InboxIntelligenceGuideV4113Test.php
          php artisan view:cache
      - name: Focused regression
        run: |
          php artisan test \
            tests/Feature/InboxIntelligenceGuideV4113Test.php \
            tests/Feature/InboxFoundationV4113Test.php \
            tests/Feature/CanoviaGuideV405Test.php \
            tests/Feature/ActionHierarchyV4113Test.php \
            tests/Feature/StudyRecallCandidateV4112Test.php \
            tests/Feature/StudyRecallV4111Test.php \
            tests/Feature/NativeAiPracticeV418Test.php
      - name: Instant Start compatibility
        run: node --test tests/js/instant-start-v391.test.mjs
